{
  "name": "git-awaken",
  "version": "1.1.0",
  "description": "The System has chosen you. Transform your GitHub activity into a dynamic, Manhwa-inspired RPG status window.",
  "main": "src/index.ts",
  "author": "BillTheDev",
  "scripts": {
    "start": "tsx --env-file=.env src/index.ts",
    "dev": "tsx --env-file=.env --watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.5.0"
  }
}